export default function Post({ id, title, content, authorName }) {
    return (
        <div>
            <p>dsfsd</p>
            <h3>{authorName}</h3>
            <h4>{title}</h4>
            <p>{content}</p>
        </div>
    );
}
